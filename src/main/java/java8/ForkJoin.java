package java8;

import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.Callable;
import java.util.concurrent.ForkJoinPool;
import java.util.function.Function;
import java.util.stream.Stream;

/**
 * Created by broniowj on 2017-02-24.
 */
public class ForkJoin {

//	Function veryLongProcessing() {
//		return () -> "a" + "b";
//	}

//	String method() {
//
////		List<ForkJoin> list = new ArrayList<>();
////		Stream<String> stream = list.parallelStream().map(veryLongProcessing());
////		Callable<List<Integer>> task = () -> stream.collect(toList());
////		ForkJoinPool forkJoinPool = new ForkJoinPool(4);
////		List<ForkJoin> newList = forkJoinPool.submit(task).get()
//	}
}
